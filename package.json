{
  "name": "minkiele.github.io",
  "version": "3.3.0",
  "license": "MIT",
  "scripts": {
    "dev": "nx run public:dev",
    "build": "nx run public:build",
    "generate": "nx run generator:generate"
  },
  "private": true,
  "devDependencies": {
    "nx": "22.3.3",
    "prettier": "^2.6.2"
  },
  "workspaces": [
    "packages/*"
  ]
}
